[tool.ruff]
line-length = 120
target-version = "py310"
exclude = [
    "venv",
    ".git",
    "tests/mock_data",
]

[tool.ruff.lint]
select = ["E", "F", "I"]
ignore = ["E501"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
asyncio_mode = "auto"
